# Rawg-clone app

to start the project,

1. first install the dependencies.

   ```shell
   cd rawg-clone
   npm i
   ```

2. simply start the project.

   ```shell
   npm run dev
   ```
